#!/bin/bash

# This script is not needed for starting the stack.
# Docker volumes are managed automatically by Docker Compose.

# Please use the Makefile to start the services, for example:
# make up-core